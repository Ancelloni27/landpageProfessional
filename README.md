Gabriel Ancelloni — Landing Page (versão simplificada)

Arquivos:
- `index.html` — conteúdo (troque foto, cv, links)
- `style.css`  — variáveis no topo para personalizar cores/raio
- `script.js`  — tema persistente e ano dinâmico

Testar localmente:
```bash
python3 -m http.server 8000
# abrir http://localhost:8000
```

Notas importantes:
- Altere `foto-perfil.jpg` e `cv.pdf` na raiz.
- Atualize o link do LinkedIn em `index.html`.
- Para resetar o tema, limpe `localStorage` (chave `theme`).

Posso substituir emojis por SVGs ou gerar um README mais detalhado se quiser.
# landpageProfessional
